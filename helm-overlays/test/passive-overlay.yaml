# Passive overlay. See the header comment in
# test/active-overlay.yaml: Operate/Tasklist and Identity/Keycloak/Optimize
# are not toggled here, since they are permanently on in
# test/east-values.yaml and test/west-values.yaml. Connectors is the only
# component that is genuinely active-passive.
connectors:
  replicas: 0
